#include <stdio.h>

#define V 3
#define VR 6

int main(int argc, char const *argv[]) {
  int vetor_a[V] , vetor_b[V] , vetor_resultado[VR];
  int i , x , y;

  printf("Digite os valores do vetor A\n");
    for(i = 0 ; i < V ; i++)
    {
      scanf("%i",&vetor_a[i]);
    }
  printf("Digite os valores do vetor B\n");
    for(i = 0 ; i < V ; i++)
    {
      scanf("%i",&vetor_b[i]);
    }

    for(i = 0)
  return 0;
}
